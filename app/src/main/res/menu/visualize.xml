<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/first"
        android:title="@string/first"
        android:icon="@drawable/ic_first_page"
        app:iconTint="?android:attr/colorControlNormal"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/prev"
        android:title="@string/previous"
        android:icon="@drawable/ic_chevron_left"
        app:iconTint="?android:attr/colorControlNormal"
        app:showAsAction="always"/>

    <item
        android:id="@+id/next"
        android:title="@string/next"
        android:icon="@drawable/ic_chevron_right"
        app:iconTint="?android:attr/colorControlNormal"
        app:showAsAction="always"/>

    <item
        android:id="@+id/last"
        android:title="@string/last"
        android:icon="@drawable/ic_last_page"
        app:iconTint="?android:attr/colorControlNormal"
        app:showAsAction="ifRoom"/>
</menu>